# 安全策略

[English](SECURITY.md) · **简体中文**

## 上报漏洞

请通过 [GitHub Security Advisories](https://github.com/asale-ai/llm-verify/security/advisories/new) 私下上报。请不要为漏洞开公开 issue。

尽量附上：受影响版本、复现步骤、影响范围。我们会在 5 个工作日内确认，15 个工作日内给出评估。

## 支持的版本

只有最新的 minor 版本会收到修复，旧版本不会。

## 本工具怎么处理你的密钥

`llm-verify` 只把你的 API Key 发到一个地方：你指定的那个端点。

- 密钥只存在于进程内存中。不写盘、不写日志，也不会出现在 HTML 或 JSON 报告里。
- `.env` 是直接读取的，**不会**导出到进程环境，所以密钥不会泄漏给子进程。
- HTTP 重定向已关闭。中转站无法把探针连同它的 `Authorization` 头弹到你没选的主机上。
- TLS 使用 `rustls` 配合系统信任库。没有跳过证书校验的选项。

报告只写到本地文件系统，不上传到任何地方。

## 报告里有什么

报告包含被测端点的响应片段、响应头、消息 ID 和 token 计数。请把报告当作对话本身来对待。

报告永远不含你的 API Key，但确实包含 base URL 与模型 ID。

## 探针安全性

探针对端点是只读的：发送聊天请求、读取响应。不尝试提权、不外泄数据、不绕过供应商的安全系统。

**探针内容刻意保持无害。** 一个相关的开源项目曾在拒绝梯度探针里放了真正有害的类别；上游供应商收紧内容管控之后，**跑那套探针导致用户账号被停权**。本项目的任何探针都必须由无害材料构成。一个可能触发供应商滥用管控的探针是缺陷，不是特性——见 [CONTRIBUTING.zh-CN.md](CONTRIBUTING.zh-CN.md) 里的规则。

有三个探针会刻意发送畸形请求：省略 API 版本头、省略鉴权、请求一个不可能存在的模型。它们都是单条格式良好的 HTTP 请求，合规端点会拒绝。这不是攻击，也不产生压力。

## 使用范围

只测试你有权测试的端点。`llm-verify` 会发出真实的计费请求：视 `--depth` 而定，一轮大约 40–60 次。把它指向你不拥有或未获授权评估的基础设施，可能违反供应商的服务条款。

## 怎么理解结果

报告是自动化黑盒测量的结果。它是证据，不是证明，更明确地不构成对任何供应商的法律指控。信号弱时工具会弃权而不是猜测；具体边界见报告内的说明与 [README.zh-CN.md](README.zh-CN.md)。
